version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing backend dependencies...
      - cd backend
      - npm install
  build:
    commands:
      - echo Build phase completed
artifacts:
  files:
    - '**/*'
